/**
 * @module coverage
 * @description Validación de cobertura mínima diaria (hard y soft).
 *              RF-16: ≥1M + ≥1T en laborables; ≥1MF + ≥1TF en fines de semana.
 *              Soft target: ≥2M + ≥2T en días laborables.
 * @dependencies business-logic (isValidShiftType)
 */

// Re-exported from generate.ts during migration; will be the canonical source
// after Task 3.4 migration is complete.
