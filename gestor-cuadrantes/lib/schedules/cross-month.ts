/**
 * @module cross-month
 * @description Continuidad entre meses: noches a mitad de bloque y packs de
 *              fin de semana incompletos que cruzan el límite de mes.
 * @dependencies generate (normalizeShift, addDays, toDateStr, fromDateStr, weekKey)
 */

// Re-exported from generate.ts during migration; will be the canonical source
// after Task 3.1 migration is complete.
