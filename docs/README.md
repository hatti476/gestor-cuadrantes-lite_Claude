# docs/

La documentación vigente de este proyecto vive en [`specs/`](../specs/) (Spec-Driven Development, ver `AGENTS.md`).

`_archive/ciclo-1/` contiene la documentación del ciclo anterior a `chore: baseline sprint-01`
(26 sprints con arquitectura multi-proyecto — `Project`/`ProjectMember`, roles `SUPER_ADMIN`/`PROJECT_ADMIN`/etc.).
Esa arquitectura fue eliminada (ver SPEC-002 a SPEC-004); se conserva como referencia histórica,
no como documentación del estado actual del sistema.
